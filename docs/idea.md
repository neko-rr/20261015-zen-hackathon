# 採用アイデア

> 候補の詳細は [ideas/01-photo-structure-for-illustrators.md](ideas/01-photo-structure-for-illustrators.md)。比較は [ideas/comparison.md](ideas/comparison.md)。

| ステータス | 採用 |
| 採用案 | [01 絵描きの資料補助](ideas/01-photo-structure-for-illustrators.md) |
| 決定日 | 2026-09-19 |
| 決定理由 | この案で進める。層分け・出典・ライセンス履歴を足した |

## 一言ピッチ

絵描きが写真資料を物体の層と関節に分け、選んだ部分を出典付きで調べ、引用に使える履歴を残せる。

## MVP（Must）

- 「はじめる」ワンクリック（または初回自動）でセッションを開始する。メール／Google ログインは不要
- 写真を1枚アップロードできる。サンプル写真は読み取り専用カタログから選び、解析結果はそのセッション内だけに残る
- 物体ごとに分け、レイヤーの表示を切り替えられる（元画像／マスク／骨格／筋肉／回転 3D）
- 人、または動物の主対象に、関節が分かる表示を重ねる（人は MediaPipe 姿勢。3D は同ポーズをドラッグ回転）
- 選んだ物体を自然言語で調べ、出典 URL 付きで短く返す
- 出典ごとに、ライセンス・作成時期・著作権の記載を抜き、履歴に残す。ページに無い項目は「未確認」
- 写真・履歴はセッションの `owner_id` 単位で隔離する。ユーザー間で共有しない
- 手元資料を `/library` に登録できる（自作／他者の二領域）。調べる・チャット時に RAG で提示する

## 技術スタック

- 実行プロダクト: Cloud Run
- AI: Gemini（検索の出典 URL のみ）。MediaPipe（人の輪郭マスクと姿勢・CPU）。Jev（引用がライセンス等を書いているか）
- 画面: 元画像／マスク／骨格／筋肉／Three.js 3D 回転
- 認証（ハッカソン）: 自前の署名付きセッション Cookie。Firebase Auth / Supabase は使わない
- 手元資料: GCS 保存＋ Vertex AI RAG Engine（ローカルは簡易索引可）。自作は `own_work`、他者は `third_party`
- 構成: [architecture.md](architecture.md)。画面と API は別の Cloud Run。Gemini は API だけ

## 今回はやらない

- 完成イラストの生成
- Kindle 本棚の自動取得（公式 API では蔵書を読めない。非公式取得はしない）
- こちらからの書籍ダウンロード。NotebookLM の非公式クライアント
- 推し活グッズ管理アプリとの統合・SSO（別プロダクト。需要確認後の任意）
- 固定の共有デモアカウント（審査員同士でデータが混ざる）
- カメラ RAW の直接登録（JPEG などに書き出してから）

## 後回し

- 動物の高精度解剖・描いた絵との反映度
- 写真ピクセルを解剖テクスチャに塗り替える生成
- 別端末でも履歴を残す本ログイン（必要になったときだけ IdP を足す）
- 古い `.doc` / `.xls` の完全対応（まずは docx / xlsx）

## 実装済み（Must 外・余力）

MVP の必須ではないが、ローカル／デモ用に入っている。詳細は [architecture.md](architecture.md)。

- 写真上の筋肉部位ラベル（姿勢からの近似。医学的正確さは保証しない）
- 手元資料 RAG（自作／他者・100MB・Office／テキスト・調べる／チャット時の自動提示）
- 手動の案件フォルダ・作品タグ（RAG のクエリ付与と案件フィルタ。AI 自動分類はしない）
- 同物体の再検索、お気に入り・出典メモ（**セッション内＋制作ログ書き出し**。クラウド永続しない）
- 編集者向け: 危険度目安、引用メモ、チェックリスト、自作／他者比較、制作ログ書き出し
